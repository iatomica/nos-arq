services:
  nos-arq:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nos-arq-app
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
